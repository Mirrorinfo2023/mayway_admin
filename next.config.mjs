/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.unsplash.com',
      'cdn.pixabay.com',
      'your-own-cdn.com',
      'picsum.photos', 
    ],
  },
};

export default nextConfig;
